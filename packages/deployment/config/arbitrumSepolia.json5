{
  // Deployment configuration for Arbitrum Sepolia (testnet)

  RewardsManager: {
    eligibilityOracle: 'RewardsEligibilityOracleMock',
  },

  IssuanceAllocator: {
    // Testnet mirror of the mainnet GIP-0089 split (mainnet is exactly 20x these
    // rates): RM 80%, InnovationAllocation 20%, total unchanged at 6.0365.
    // RecurringAgreementManager stays at 0 — DIPs dormant.
    issuancePerBlock: '6.0365',
    allocations: {
      RewardsManager: { selfGrtPerBlock: '4.8292' },
      RecurringAgreementManager: { allocatorGrtPerBlock: '0' },
      InnovationAllocation: { allocatorGrtPerBlock: '1.2073' },
    },
  },
}
