# Issuance Deployment Docs (OBSOLETE)

The canonical documentation for issuance deployment now lives under
`packages/issuance/deploy/docs` and `packages/deploy/docs`.

Start with:

- `packages/issuance/deploy/docs/Design.md` (canonical issuance deployment design)
- `packages/deploy/DeploymentGuide.md` (orchestration / governance flows)

This `incoming/issuance` directory is retained only as a pointer; see git
history for the original contents.
